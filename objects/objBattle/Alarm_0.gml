instance_create_depth(x, y, -99999, objRoomTransitionFade);
instance_create_depth(x, y, -99999, objPartyLevels);
canDraw = true;

with (objParentDecoration)
{
	visible = false;
}
with (objParentInteractable)
{
	visible = false;	
}