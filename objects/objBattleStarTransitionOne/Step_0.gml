if (image_xscale < 1 and image_yscale < 1)
{
	image_xscale += 0.1;
	image_yscale += 0.1;
	image_alpha += 0.1;
}


if (expandAgain)
{
	image_xscale += 0.25;
	image_yscale += 0.25;
	image_angle += 5;
}
